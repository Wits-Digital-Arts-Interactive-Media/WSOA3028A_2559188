//create varibles of sum
let num1 = 2;
let num2 = 3;

//Compute the sum of the two variables
let sum = num1 + num2;

//Use "if" statement to check if the variables are equal, then return the sum if the variables are not equal.
if (num1 === num2){
    return sum * 3;
}
 else {
    return sum;
 }
